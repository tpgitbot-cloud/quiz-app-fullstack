{
  "name": "quiz-app",
  "version": "1.0.0",
  "description": "Secure Quiz Application for Web and Android",
  "main": "index.js",
  "scripts": {
    "dev": "python -m http.server 8000",
    "build": "npx cap update && npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
